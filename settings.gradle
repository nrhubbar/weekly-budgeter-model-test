pluginManagement {
    plugins {
        id 'software.amazon.smithy' version '0.7.0'
    }
}

rootProject.name = 'weekly-budgeter-v2'

includeBuild('./model')

include 'server-codegen'
project(':server-codegen').projectDir = file('server/codegen')

